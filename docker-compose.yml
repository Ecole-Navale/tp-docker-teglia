services:
  web:
    image: vote-apache
    ports:
      - "80:80"

  db:
    image: vote-mysql
    environment:
      MYSQL_ROOT_PASSWORD: P@ssw0rdEN24

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: P@ssw0rdEN24
    depends_on:
      - db
